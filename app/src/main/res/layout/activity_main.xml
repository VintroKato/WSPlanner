<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_coordinator"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/app_bg">

    <LinearLayout
        android:id="@+id/main_content_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Top Header Toolbar -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="16dp"
            android:paddingTop="12dp"
            android:paddingEnd="16dp"
            android:paddingBottom="8dp">

            <!-- Open Excel File Button -->
            <ImageButton
                android:id="@+id/button_open_file"
                android:layout_marginTop="2dp"
                android:layout_width="38dp"
                android:layout_height="38dp"
                android:layout_alignParentStart="true"
                android:layout_centerVertical="true"
                android:background="@drawable/bg_badge_rect"
                android:backgroundTint="?attr/card_bg"
                android:contentDescription="Open Excel schedule file"
                android:src="@drawable/ic_file_excel" />

            <!-- App Title -->
            <TextView
                android:id="@+id/app_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:text="WSPlanner"
                android:textColor="?attr/app_text"
                android:textSize="17sp"
                android:textStyle="bold" />

            <!-- Menu Button (Three dots) -->
            <ImageButton
                android:id="@+id/button_menu"
                android:layout_width="38dp"
                android:layout_height="38dp"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Menu"
                android:src="@drawable/ic_more_vert" />
        </RelativeLayout>

        <!-- Date Navigation Bar -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingBottom="8dp">

            <!-- Segmented Date Selector Container -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="40dp"
                android:layout_weight="1"
                android:background="@drawable/bg_badge_rect"
                android:backgroundTint="?attr/card_bg"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <!-- Previous Day Button -->
                <ImageButton
                    android:id="@+id/button_prev_day"
                    android:layout_width="38dp"
                    android:layout_height="match_parent"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Previous day"
                    android:src="@drawable/ic_chevron_left" />

                <!-- Return to Today Button (Visible only when not today) -->
                <ImageButton
                    android:id="@+id/button_today"
                    android:layout_width="36dp"
                    android:layout_height="match_parent"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Return to today"
                    android:src="@drawable/ic_restore"
                    android:visibility="invisible" />

                <!-- Selected Date Label (Click opens calendar) -->
                <TextView
                    android:id="@+id/selected_date_text"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1"
                    android:gravity="center"
                    android:clickable="true"
                    android:focusable="true"
                    android:background="?attr/selectableItemBackground"
                    android:text="Środa, 02.09"
                    android:textColor="?attr/app_text"
                    android:textSize="14sp"
                    android:textStyle="bold" />

                <!-- Next Day Button -->
                <ImageButton
                    android:id="@+id/button_next_day"
                    android:layout_width="38dp"
                    android:layout_height="match_parent"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="Next day"
                    android:src="@drawable/ic_chevron_right" />
            </LinearLayout>

            <!-- Calendar Picker Button -->
            <ImageButton
                android:id="@+id/button_calendar_picker"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/bg_badge_rect"
                android:backgroundTint="?attr/card_bg"
                android:contentDescription="Open calendar picker"
                android:src="@drawable/ic_calendar" />
        </LinearLayout>

        <!-- Last Sync Info Label -->
        <TextView
            android:id="@+id/last_sync_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:alpha="0.55"
            android:gravity="center"
            android:text="Zaktualizowano: dzisiaj, 18:03"
            android:textColor="?attr/app_text"
            android:textSize="11sp" />

        <!-- SwipeRefreshLayout with RecyclerView -->
        <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
            android:id="@+id/swipe_refresh_layout"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recycler_schedule"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:paddingBottom="32dp" />

        </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>
    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
